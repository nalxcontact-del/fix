# PersonaChat v86.1 — OSINT Build Fix

Fixes the duplicate `plan` declaration in `lib/server/osint-tavily.ts` by renaming the request research plan to `osintPlan` consistently inside `refreshOsintForCharacter`.

No OSINT policy, provider selection, credit logic, caching behavior, UI, or dependency versions were changed.
